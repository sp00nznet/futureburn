<Window x:Class="Futureburn.Gui.TextInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input"
        Height="170" Width="460"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <TextBlock x:Name="PromptText" Margin="0,0,0,6"/>
    <TextBox x:Name="ValueBox" Grid.Row="1" Padding="4" KeyDown="ValueBox_KeyDown"/>
    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button Content="Cancel" IsCancel="True" Click="Cancel_Click" Padding="20,4" Margin="0,0,8,0"/>
      <Button Content="OK"     IsDefault="True" Click="Ok_Click"     Padding="20,4"/>
    </StackPanel>
  </Grid>
</Window>
